
const Hero = () => {
    return (
        <div className="h-screen w-full hero-bg-gradient relative -z-20">
            <div className="absolute h-screen inset-0 bg-gradient-to-b from-transparent via-background -z-10 to-background pointer-events-none" />
        </div>
    )
}

export default Hero